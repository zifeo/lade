> ./show-example intermediate
^printenv Authorization user:
  user: demo-user
  .password: vault://127.0.0.1:8200/secret/password/value1
  Authorization: 'sh://printf "Basic %s" "$(printf "%s:%s" "${user}" "${.password}" | base64 | tr -d "\n")"'

> printenv Authorization user
⠋ Vault 127.0.0.1:8200: .password

⠋ sh printf "Basic %s" "$(printf "%s:%s" "${user}" "${.password}" | base64 | tr

-d "\n")": Authorization

✔︎ Vault 127.0.0.1:8200: .password                                       14 ms

✔︎ sh printf "Basic %s" "$(printf "%s:%s" "${user}" "${.password}" | base64 | tr

-d "\n")": Authorization 14 ms

Basic ZGVtby11c2VyOml0c2FzZWNyZXQ=

>
